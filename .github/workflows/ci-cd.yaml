name: Continues Integration and Deployment
on:
  push:
    branches:
      - ftp_2
jobs:
  Deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check Out Code 🚓
        uses: actions/checkout@v3
      
      - name: Install FTP client
        run: sudo apt-get install -y lftp

      - name: Deploy code to FTP server
        run: |
          lftp -c "set ftp:ssl-allow no; open -u u623109200.akhtarmas,Boingo371! ftp.icreativecode.com; mirror -R ./test ./"