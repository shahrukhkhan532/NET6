# For auto generating this yaml file for .NET project run these command sequencely
#dotnet new workflow --search
#dotnet new --install TimHeuer.GitHubActions.Templates
#dotnet new workflow

name: Continues Integration and Deployment for Self Runner

on:
  push:
    branches:
      - dummyBranch

env:
  DOTNET_VERSION: "6.0.x"
  AZURE_WEB_APP_NAME: rainers-first-webapp
  TEAM_URL: "https://icaremanagerllc.webhook.office.com/webhookb2/1c9fa85a-a9db-4824-a749-51e475101ae5@89e9d208-fb4a-4579-a02e-ae453b60c3a6/IncomingWebhook/2518c48279da4630b2ad749568c4bf31/22a4babb-2d32-4af5-bb41-4df9d477e8a8"

jobs:
  build_test_deploy:
    runs-on:
      - self-hosted
      - label-test
    steps:
      - name: Notify Development Team 🔔
        shell: cmd
        run: |
          curl POST ${{ env.TEAM_URL }}
            -H "Content-Type: application/json"
            -d '{ "title":"test", "text":"Deployment done by Dev" }'

      - name: Check Out Code 🚓
        uses: actions/checkout@v3

      - name: Restore Dependencies ⌚
        run: dotnet restore

      - name: Build App 🚀
        run: dotnet build -c Release --no-restore

      - name: Publish App 📑
        run: |
          echo ${{ github.actor }}
          dotnet publish -c Release -o ./out

      - name: Stop IIS Server 🛑
        shell: cmd
        run: iisreset /stop

      - name: Deploy to IIS 🤩🤟
        shell: PowerShell
        run: Copy-Item ".\out\*" -Destination "c:\www\gh-actions-iis-demo.com" -Recurse

      - name: Start IIS Server ✔
        shell: cmd
        run: iisreset /start
